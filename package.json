{
  "name": "biorxiv-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=./packages/*",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^24.2.1",
    "esbuild": "^0.25.9",
    "eslint-config-curvenote": "^0.0.4",
    "npm-run-all": "^4.1.5",
    "prettier": "latest",
    "rimraf": "^6.0.1",
    "turbo": "^2.5.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "tsx": "^4.19.2"
  },
  "packageManager": "npm@10.0.0"
}
